import Header from "../layout/header";
import Packages from "../layout/packages";

const Services = () => {
  return (
    <section>
      <Header />
      <Packages />
    </section>
  );
};

export default Services;
